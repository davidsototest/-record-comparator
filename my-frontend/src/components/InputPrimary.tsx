
import React from 'react';

interface Props {
    // Define props here
}

const InputPrimary: React.FC<Props> = (Props) => {
    return (
        <div>
            <h1> test </h1>
        </div>
    );
};

export default InputPrimary;