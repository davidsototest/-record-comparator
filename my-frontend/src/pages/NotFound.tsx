
import React from 'react';

interface Props {
    // Define props here
}

const NotFound: React.FC<Props> = (Props) => {
    return (
        <div>
            <h1> NotFound </h1>
        </div>
    );
};

export default NotFound;